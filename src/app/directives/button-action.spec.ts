import { ButtonAction } from './button-action';

describe('ButtonAction', () => {
  it('should create an instance', () => {
    const directive = new ButtonAction();
    expect(directive).toBeTruthy();
  });
});
