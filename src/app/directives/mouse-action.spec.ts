import { MouseAction } from './mouse-action';

describe('MouseAction', () => {
  it('should create an instance', () => {
    const directive = new MouseAction();
    expect(directive).toBeTruthy();
  });
});
