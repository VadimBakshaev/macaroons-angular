import { PriceTrasformPipe } from './price-trasform-pipe';

describe('PriceTrasformPipe', () => {
  it('create an instance', () => {
    const pipe = new PriceTrasformPipe();
    expect(pipe).toBeTruthy();
  });
});
